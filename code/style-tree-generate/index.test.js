const getCommonStyle = require('./getCommonStyle')
const { styleTree1, styleTree2 } = require('./data')
const root1 = {
  props: {
    type: 'default',
    shape: 'square',
  },
  style: {},
}

test('组件样式生成', () => {})
